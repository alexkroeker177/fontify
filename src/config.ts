// Site links & integrations. Empty values hide the corresponding UI.
// Set via .env / build environment - see .env.example.
export const KOFI_URL: string = import.meta.env.VITE_KOFI_URL ?? ''
export const GITHUB_URL: string = import.meta.env.VITE_GITHUB_URL ?? ''
